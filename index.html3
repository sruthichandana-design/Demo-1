<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>APEX SPORTS - Peak Performance Gear</title>
    <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Roboto+Condensed:wght@300;400;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --volt: #D4FF00;
            --fire-red: #FF3E3E;
            --deep-black: #0A0A0A;
            --concrete: #E8E8E8;
            --steel-blue: #00A8E8;
            --dark-gray: #1A1A1A;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Roboto Condensed', sans-serif;
            background: var(--deep-black);
            color: var(--concrete);
            overflow-x: hidden;
        }

        header {
            position: fixed;
            top: 0;
            width: 100%;
            padding: 1rem 3rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 1000;
            background: rgba(10, 10, 10, 0.95);
            backdrop-filter: blur(10px);
            border-bottom: 2px solid var(--volt);
        }

        .logo {
            font-family: 'Bebas Neue', sans-serif;
            font-size: 2.5rem;
            letter-spacing: 0.15rem;
            color: var(--volt);
            text-shadow: 0 0 20px rgba(212, 255, 0, 0.5);
        }

        .nav-container {
            display: flex;
            align-items: center;
            gap: 3rem;
        }

        nav {
            display: flex;
            gap: 2.5rem;
        }

        nav a {
            color: var(--concrete);
            text-decoration: none;
            font-weight: 700;
            font-size: 0.95rem;
            text-transform: uppercase;
            letter-spacing: 0.1rem;
            position: relative;
            transition: color 0.3s ease;
        }

        nav a::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 3px;
            background: var(--volt);
            transition: width 0.3s ease;
        }

        nav a:hover {
            color: var(--volt);
        }

        nav a:hover::after {
            width: 100%;
        }

        .search-icon {
            width: 40px;
            height: 40px;
            background: var(--fire-red);
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            font-size: 1.2rem;
            transition: all 0.3s ease;
            clip-path: polygon(0 0, 100% 0, 85% 100%, 0% 100%);
        }

        .search-icon:hover {
            background: var(--volt);
            color: var(--deep-black);
        }

        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            background: 
                linear-gradient(135deg, rgba(212, 255, 0, 0.1) 0%, transparent 50%),
                linear-gradient(225deg, rgba(255, 62, 62, 0.1) 0%, transparent 50%);
            overflow: hidden;
        }

        .diagonal-stripe {
            position: absolute;
            width: 200%;
            height: 30px;
            background: var(--volt);
            transform: rotate(-15deg);
            opacity: 0.1;
        }

        .stripe-1 { top: 20%; animation: slideStripe 15s linear infinite; }
        .stripe-2 { top: 50%; animation: slideStripe 20s linear infinite; animation-delay: -5s; }
        .stripe-3 { top: 80%; animation: slideStripe 18s linear infinite; animation-delay: -10s; }

        @keyframes slideStripe {
            from {
                transform: rotate(-15deg) translateX(-100%);
            }
            to {
                transform: rotate(-15deg) translateX(100%);
            }
        }

        .hero-content {
            text-align: center;
            z-index: 2;
            padding: 0 2rem;
        }

        .hero-badge {
            display: inline-block;
            padding: 0.5rem 1.5rem;
            background: var(--fire-red);
            color: white;
            font-weight: 700;
            font-size: 0.85rem;
            letter-spacing: 0.15rem;
            margin-bottom: 2rem;
            clip-path: polygon(5% 0%, 100% 0%, 95% 100%, 0% 100%);
            animation: pulse 2s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.05);
            }
        }

        .hero h1 {
            font-family: 'Bebas Neue', sans-serif;
            font-size: 8rem;
            line-height: 0.9;
            margin-bottom: 1.5rem;
            letter-spacing: 0.1rem;
            background: linear-gradient(135deg, var(--volt) 0%, var(--steel-blue) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-shadow: 0 0 40px rgba(212, 255, 0, 0.3);
            animation: heroGlow 3s ease-in-out infinite alternate;
        }

        @keyframes heroGlow {
            from {
                filter: drop-shadow(0 0 20px rgba(212, 255, 0, 0.3));
            }
            to {
                filter: drop-shadow(0 0 40px rgba(0, 168, 232, 0.5));
            }
        }

        .hero-tagline {
            font-size: 1.3rem;
            color: #AAA;
            margin-bottom: 3rem;
            font-weight: 300;
            letter-spacing: 0.05rem;
        }

        .cta-button {
            display: inline-block;
            padding: 1.2rem 3.5rem;
            background: var(--volt);
            color: var(--deep-black);
            text-decoration: none;
            font-weight: 700;
            font-size: 1.1rem;
            text-transform: uppercase;
            letter-spacing: 0.15rem;
            position: relative;
            overflow: hidden;
            clip-path: polygon(5% 0%, 100% 0%, 95% 100%, 0% 100%);
            transition: all 0.3s ease;
        }

        .cta-button::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: var(--fire-red);
            transition: left 0.4s ease;
        }

        .cta-button span {
            position: relative;
            z-index: 1;
        }

        .cta-button:hover::before {
            left: 0;
        }

        .cta-button:hover {
            color: white;
        }

        .categories {
            padding: 6rem 3rem;
            background: var(--dark-gray);
        }

        .section-header {
            text-align: center;
            margin-bottom: 4rem;
        }

        .section-header h2 {
            font-family: 'Bebas Neue', sans-serif;
            font-size: 4rem;
            letter-spacing: 0.15rem;
            margin-bottom: 1rem;
        }

        .section-header p {
            color: #888;
            font-size: 1.1rem;
        }

        .category-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 2rem;
            max-width: 1400px;
            margin: 0 auto;
        }

        .category-card {
            background: var(--deep-black);
            padding: 3rem 2rem;
            text-align: center;
            position: relative;
            overflow: hidden;
            border: 2px solid transparent;
            transition: all 0.4s ease;
            cursor: pointer;
        }

        .category-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 0;
            background: linear-gradient(135deg, var(--volt), var(--steel-blue));
            transition: height 0.4s ease;
        }

        .category-card:hover::before {
            height: 100%;
        }

        .category-card:hover {
            border-color: var(--volt);
            transform: translateY(-10px);
        }

        .category-icon {
            font-size: 3.5rem;
            margin-bottom: 1.5rem;
            position: relative;
            z-index: 1;
            transition: transform 0.4s ease;
        }

        .category-card:hover .category-icon {
            transform: scale(1.2) rotate(5deg);
        }

        .category-card h3 {
            font-family: 'Bebas Neue', sans-serif;
            font-size: 1.8rem;
            letter-spacing: 0.1rem;
            margin-bottom: 0.5rem;
            position: relative;
            z-index: 1;
        }

        .category-card p {
            font-size: 0.9rem;
            color: #888;
            position: relative;
            z-index: 1;
        }

        .promo-banner {
            background: linear-gradient(135deg, var(--fire-red) 0%, var(--steel-blue) 100%);
            padding: 4rem 3rem;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .promo-banner::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: repeating-linear-gradient(
                45deg,
                transparent,
                transparent 10px,
                rgba(255, 255, 255, 0.05) 10px,
                rgba(255, 255, 255, 0.05) 20px
            );
        }

        .promo-content {
            position: relative;
            z-index: 1;
        }

        .promo-banner h2 {
            font-family: 'Bebas Neue', sans-serif;
            font-size: 4.5rem;
            letter-spacing: 0.2rem;
            margin-bottom: 1rem;
        }

        .promo-banner p {
            font-size: 1.3rem;
            margin-bottom: 2rem;
            font-weight: 300;
        }

        .stats-section {
            padding: 5rem 3rem;
            background: var(--deep-black);
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 3rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        .stat-card {
            text-align: center;
            padding: 2rem;
            border-left: 4px solid var(--volt);
        }

        .stat-number {
            font-family: 'Bebas Neue', sans-serif;
            font-size: 4.5rem;
            color: var(--volt);
            margin-bottom: 0.5rem;
        }

        .stat-label {
            font-size: 1.1rem;
            text-transform: uppercase;
            letter-spacing: 0.1rem;
            color: #888;
        }

        @media (max-width: 768px) {
            .hero h1 {
                font-size: 4rem;
            }
            
            .category-grid {
                grid-template-columns: 1fr;
            }
            
            .stats-section {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <header>
        <div class="logo">APEX SPORTS</div>
        <div class="nav-container">
            <nav>
                <a href="#men">Men</a>
                <a href="#women">Women</a>
                <a href="#sports">Sports</a>
                <a href="#gear">Gear</a>
                <a href="#sale">Sale</a>
            </nav>
            <div class="search-icon">🔍</div>
        </div>
    </header>

    <section class="hero">
        <div class="diagonal-stripe stripe-1"></div>
        <div class="diagonal-stripe stripe-2"></div>
        <div class="diagonal-stripe stripe-3"></div>
        
        <div class="hero-content">
            <div class="hero-badge">NEW ARRIVALS</div>
            <h1>UNLEASH YOUR<br>POTENTIAL</h1>
            <p class="hero-tagline">Premium athletic gear engineered for champions</p>
            <a href="#shop" class="cta-button">
                <span>Shop Now</span>
            </a>
        </div>
    </section>

    <section class="categories">
        <div class="section-header">
            <h2>Shop By Sport</h2>
            <p>Find gear designed for your passion</p>
        </div>
        <div class="category-grid">
            <div class="category-card">
                <div class="category-icon">⚽</div>
                <h3>Football</h3>
                <p>Boots & Training Gear</p>
            </div>
            <div class="category-card">
                <div class="category-icon">🏀</div>
                <h3>Basketball</h3>
                <p>Shoes & Apparel</p>
            </div>
            <div class="category-card">
                <div class="category-icon">🏃</div>
                <h3>Running</h3>
                <p>Performance Tech</p>
            </div>
            <div class="category-card">
                <div class="category-icon">🏋️</div>
                <h3>Training</h3>
                <p>Gym Essentials</p>
            </div>
        </div>
    </section>

    <section class="promo-banner">
        <div class="promo-content">
            <h2>WINTER SALE</h2>
            <p>Up to 50% off on selected items</p>
            <a href="#sale" class="cta-button">
                <span>Shop Sale</span>
            </a>
        </div>
    </section>

    <section class="stats-section">
        <div class="stat-card">
            <div class="stat-number">10K+</div>
            <div class="stat-label">Products</div>
        </div>
        <div class="stat-card">
            <div class="stat-number">500+</div>
            <div class="stat-label">Brands</div>
        </div>
        <div class="stat-card">
            <div class="stat-number">1M+</div>
            <div class="stat-label">Athletes</div>
        </div>
    </section>
</body>
</html>
